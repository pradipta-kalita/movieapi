spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/movies
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  servlet:
    multipart:
      enabled: true


cloudinary:
  cloud-name:
  api-key:
  api-secret:

security:
  jwt:
    secret:
    expiration: 3600000
  refreshToken:
    expiration: 86400000

resend:
  api-key:
  from-email: onboarding@resend.dev